# iPhone release preparation

Status recorded September 23, 2026. **Build 4 uploaded and processed successfully by Apple; ready for internal TestFlight testing. Not submitted to App Review or released on the App Store.**

## Release identity

| Field | Value |
| --- | --- |
| Product | MLX Peer |
| iOS version / build | 0.2.0 / 4 |
| Bundle identifier | `dev.mlxpeer.worker.samuelreyes` |
| App Store Connect ID | `6815283902` |
| Minimum iOS | 17.0 |
| Built with | Xcode 26.6, iOS 26.5 SDK |
| Initial platform | iPhone |
| Mac companion | [0.2.0-alpha.3](https://github.com/samuelreyes982/mlx-peer/releases/tag/v0.2.0-alpha.3) |

The alpha.3 Mac package updates the icon and outer app version. Its bundled inference engine is unchanged from alpha.2 and still reports that version.

## Completed

- Original opaque 1024-pixel iOS icon and matching Mac ICNS, generated by `scripts/generate_icons.swift`.
- Release iPhone archive and Apple Distribution-signed IPA. Local signature verification passed, debugging entitlement is false, and the embedded profile is for App Store distribution.
- Upload accepted by Apple on September 23 at 9:50 AM PDT; processing is Complete. Build 4 is attached to the 0.2.0 store draft and the internal **MLX Peer Release Check** TestFlight group. The group has no testers yet; automatic distribution is disabled.
- Privacy manifest and bundled dependency acknowledgements. Debug fixture controls and pairing-report launch flags are excluded from Release.
- In-app setup, privacy, support and license screens.
- Three genuine iPhone simulator screenshots at 1320 × 2868, uploaded in Connect / Setup / Privacy order. No staged inference result is shown.
- English store copy in `en-US/`, public [support](../docs/SUPPORT.md) and [privacy](../docs/PRIVACY.md) pages, and [review instructions](REVIEW_NOTES.md).
- App Store Connect draft for 0.2.0, categories Utilities / Developer Tools, [marketing website](https://mlx-peer.com), and manual release selected.
- **Data Not Collected** privacy responses published with the developer's approval. See the [source audit](PRIVACY_AUDIT.md).
- Apple-generated 4+ rating for the current iPhone pairing/status interface, which does not display model responses or a public content feed. Reassess if chat or other content is added to iPhone.
- Mac and Apple Vision Pro availability disabled for the iOS app. The separate native Mac companion is distributed through GitHub.
- Release build installed and tested on a physical iPhone 16 over USB with the packaged Mac engine. Pairing reuse, arithmetic, cancellation, chat after cancellation, oversized-prompt rejection, and reconnect/cached reload passed. [Machine-readable report](../docs/companion-release-build4-20260923.json).

The signed IPA's SHA-256 is:

```text
d31cfc6078fcf1873fe707c23f15bbe47422918a391acbbe9e73e95a6fa2f4c1
```

The physical-device test used a development-signed Release installation. A TestFlight installation of the exported distribution build has not yet been tested. Screenshots were taken from the Release UI in Simulator; MLX inference was verified on the physical phone.

## Remaining before public release

1. Add the developer to the internal TestFlight group, install the processed build through TestFlight, and repeat the complete install, pairing, model transfer, cancellation and reconnection flow, including a fresh installation.
2. Developer ID-sign and notarize the Mac companion, then validate its download on a clean Mac. The current GitHub preview is ad-hoc signed. The inside-out signing workflow in `scripts/sign_macos.py` passed signature verification and engine startup using an existing Apple Development identity, but Developer ID signing, notarization, and a fresh USB runtime check remain. Do not ask reviewers or users to disable Gatekeeper.
3. Complete the review contact, launch price/regions, and the developer's content-rights declaration in App Store Connect. Contact details belong in Apple's private form, not this repository.
4. Replace the pre-submission warning in review notes with the final notarized Mac download and working demonstration instructions. Supply any hardware or demonstration Apple requests.
5. Submit for App Review only after those items are complete. Manual release remains a separate step after approval.

No model is bundled. Users obtain a supported checkpoint separately under its license. No measured speed, energy, cobalt, hardware-purchase or e-waste savings are claimed. The tested link is USB, not Thunderbolt, and Wi-Fi inference is not supported.

## Rebuild and export

Use the Xcode version above, the pinned Swift packages, your authorized signing identity, and the matching provisioning profile. The repository contains no private signing keys or reusable account credentials. Review package build plugins when Xcode asks.

```sh
xcodebuild \
  -project ios/App/MLXPeerApp.xcodeproj \
  -scheme MLXPeerApp \
  -configuration Release \
  -destination 'generic/platform=iOS' \
  -archivePath build/MLXPeer.xcarchive \
  archive

xcodebuild \
  -exportArchive \
  -archivePath build/MLXPeer.xcarchive \
  -exportOptionsPlist ios/App/ExportOptions-AppStore.plist \
  -exportPath build/AppStoreExport
```

`ExportOptions-AppStore.plist` names the publisher's team and the `MLX Peer App Store` profile. Forks must replace those with their own team, bundle ID and profile. Exporting creates a local artifact; it does not upload or publish it.
